# Agent memory

## Learned User Preferences

## Learned Workspace Facts

- When merging main into a feature branch, bump the package version in pyproject.toml (and CHANGELOG.md, uv.lock) so it is greater than main; the CI version-check job otherwise fails.
