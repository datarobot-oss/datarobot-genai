---
# https://taskfile.dev
version: '3'

dotenv: [".env"]

includes:
  dragent:
    taskfile: dragent/Taskfile.yaml
  lint:
    taskfile: ../.taskfiles/lint.yml
    flatten: true
  license:
    taskfile: ../.taskfiles/license.yml
    flatten: true

tasks:
  install:
    desc: "🔧 Install dependencies for a specific group of functionality"
    requires:
      vars:
      - name: GROUP
        enum:
          - dragent-nat
    cmds:
      - echo "🔧 Installing dependencies for {{.GROUP}}"
      - uv sync --group {{.GROUP}}
