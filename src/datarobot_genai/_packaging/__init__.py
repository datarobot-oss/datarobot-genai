"""Packaging helpers for datarobot-genai.

Ships the canonical uv workspace-root configuration that consumers must apply
themselves (uv does not propagate override/exclude/constraint settings through
package metadata) and exposes it via the ``datarobot-genai-uv-config`` command.
"""
